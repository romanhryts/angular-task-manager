import { WindowClickDetectDirective } from './window-click-detect.directive';

describe('WindowClickDetectDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowClickDetectDirective();
    expect(directive).toBeTruthy();
  });
});
